import Foundationfunc luckyTicket (_ number: Int) -> Bool {    let strNumber = String(number)    let charCounter = strNumber.count    guard charCounter % 2 == 0 && charCounter > 1 && charCounter <= 8 else {        print("This ticket is uncorrect to the condition of the task")              return false    }    var (tempNumber, leftSum, rightSum) = (strNumber, 0, 0)    for _ in 0..<charCounter / 2 {        leftSum += Int(String(tempNumber.first!))!        tempNumber.removeFirst()    }    for _ in 0..<charCounter / 2 {        rightSum += Int(String(tempNumber.first!))!        tempNumber.removeFirst()    }        return leftSum == rightSum} let firstTicket = 90324532let secondTicket = 110001let thirdTicket = 23456luckyTicket(firstTicket)luckyTicket(secondTicket)luckyTicket(thirdTicket)